# OpenClaw Stack - Variaveis de Ambiente
# ============================================
# Gerado automaticamente pelo setup.sh
# Para configuracao manual, copie para .env e preencha os valores.

# Token de autenticacao do Gateway (gerado pelo setup.sh)
OPENCLAW_GATEWAY_TOKEN=

# Dominio para acesso via Traefik (ex: openclaw.meudominio.com.br)
OPENCLAW_DOMAIN=

# Nome do stack no Docker Swarm (padrao: openclaw)
OPENCLAW_STACK_NAME=openclaw

# Nome da rede overlay do Traefik (padrao: traefik-public)
OPENCLAW_TRAEFIK_NETWORK=traefik-public

# Caminho absoluto da instalacao (preenchido pelo setup.sh)
OPENCLAW_BASE_PATH=
